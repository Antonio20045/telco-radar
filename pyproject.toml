[project]
name = "telco-radar"
version = "0.1.0"
description = "Global telco competitive-intelligence agent pipeline"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
