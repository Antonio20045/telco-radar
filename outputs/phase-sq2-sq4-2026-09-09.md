# Phase S-Q2/S-Q4 — Abschlussbericht (R2, 09.09.2026)

Auftragsgrundlage: `BRIEF_SQ2_SQ4_R2.md` (R2-Abschnitt 0 + Original-Kriterien 1–5).
Branch: `openclaw/ticket-sq2-sq4` auf main `d26617e`. Commits dieser Phase:

| Commit | Inhalt |
|---|---|
| `cccf6ba` (R1, 03:21) | S-Q2 Stat-Zeile: `tarife_view.py` Bilanz, `tarife.html.j2`, gestellter Wahrheitstest in `test_effektivpreis.py` |
| `47cff2a` (R2) | S-Q4 Bindungs-Terminologie: `effektivpreis.py`, `geraete_tco_grafik.py`, `geraete.html.j2`, drei Testdateien |
| `1ae05f3` (R2) | S-Q2-Nachtrag: Docstring-Fund + Echtbestand-Wahrheitstest |
| `5241326` (R2) | Site-Render (tarife.html erstmals mit cccf6ba-Wortlaut, geraete.html mit S-Q4-Wortlaut) |

**Suite am Ende: 2884 passed / 0 failed / 12 skipped** (Baseline auf main `d26617e`:
2881/0/12; +1 aus `cccf6ba`, +2 aus R2). `pruefe_portal.py` wurde nicht gewälzt —
seine Kriterien kennen keines der geänderten Etikette (diff-reviewer, Punkt 5).

---

## 0. Was aus dem R1-WIP wurde (übernommen / verworfen / ergänzt)

Kein Rescue-Commit seit 03:27 (`git log` geprüft) — der Baum war exakt der
R1-Stand: 1 Commit + 4 uncommittierte Dateien. Sichtung per `git diff`:

| R1-WIP | Entscheidung | Grund |
|---|---|---|
| `effektivpreis.py`: Laufzeit-Merkmal „N Monate Bindung" → „N Monate Mindestlaufzeit" | **übernommen** | Zahl und Bedingung unverändert, nur Benennung; das Wort „Mindestlaufzeit" steht schon im Abschnitt „Was diese Zahlen nicht koennen" der Seite |
| `geraete_tco_grafik.py`: G1-Gruppenkopf „N Monate Bindung" → „gerechnet über N Monate", aria-label entschlackt | **übernommen** | G1 wird seit BRIEF_FADEN (05.09.) nicht gerendert — der Code haelt die Terminologie fuer seine Rueckkehr; die Referenzlinien-Bedingung (`tarif_monate != laufzeit`) unangetastet |
| `geraete.html.j2`: fuenf Stellen (BAU-Zeile, Monatsraten-Label, Rechenweg-Satz, Modellkopf, Sortier-Option) | **übernommen, zwei Stellen verbessert** | siehe unten |
| `test_geraete_tco_hauptansicht.py`: 2 Assertions auf neuen Wortlaut | **übernommen** | jede folgt dem neuen Wortlaut, keine geschwaechcht (die eine erhaelt ZUSAETZLICH `assert "Monate Bindung" not in svg`) |
| BAU-Klammerteil „in der Zahl 24 davon" | **umformuliert** → „(24 davon in der TCO-24)" | „in der Zahl" benennt keine Zahl — die Karte heisst ihre Leitzahl aber beim Namen |
| Bedingung `k.raten_laufzeit != k.laufzeit` im Klammerteil | **korrigiert** → `>` | diff-reviewer S3: ein Bündel mit kürzerem Ratenende druckte sonst „12 Monate (24 davon …)" — 24 Raten von 12; am echten Bestand byte-neutral (alle 36>24) |
| angekündigt, nicht angekommen: „neue Wahrheitstest-Datei für die Stat-Zeile (echter Bestand, F5-Muster)" | **nachgeholt** als `test_die_stat_zeile_am_echten_bestand` in `test_effektivpreis.py` | keine eigene Datei noetig — der gestellte Zwilling steht im selben Modul; F5-Muster = Rechnung gegen `data/state/tarife.jsonl` statt gegen eine Fixture |

**Verworfen: nichts.** Der WIP enthielt keinen kaputten Inhalt — er war unvollständig
(S-Q4 uncommittet, der Echtbestand-Test fehlte), aber in jeder geprueften Zahl richtig.

Zwei Docstring-Funde von R2 dazu (beide im Gefolge von cccf6ba, keine neue Funktion):
`tarife_view.py` behauptete noch die „eigene Liste" fuer unbegrenzte Tarife, die cccf6ba
gerade aus dem Template entfernt hatte; `effektivpreis.py` zaehlte in seinem Docstring
noch die „Laufzeitbindung" unter den Merkmalen, die jetzt Mindestlaufzeit heisst.

---

## 1. S-Q2 Stat-Zeile — was die zwei Zahlen tatsaechlich bedeuten

**Vorher** (gerendert bis 08.09.) / **Nachher** (ab `cccf6ba` + `5241326`):

> <b>52</b> „mit offenen Punkten (z. B. Cashback)" / <b>38</b> „davon in der Karte"
> → <b>40</b> „ohne Anschlusspreis-Angabe" / <b>38</b> „mit Datenvolumen in der Karte"
> plus Hinweissatz: „… 12 Tarife nennen keins, 2 laufen ohne Grenze – beide stehen mit
> Preis und Effektivpreis in der Tabelle unten, nur ohne Ort auf der Volumenachse."

**Was die Zahlen bedeuten, aus dem Code ermittelt** (Verifikation von R2, nicht dem
Commit-Text geglaubt — am echten Bestand `data/state/tarife.jsonl`, Stand 2026-09-08,
nachgerechnet mit `tarife_view.aufbereiten`):

| Zahl | Code-Stelle | Bedeutung, am Bestand gemessen |
|---|---|---|
| alt „52 mit offenen Punkten" | `sum(1 for z in zeilen if z["luecken"])` — jede Zeile mit IRGENDEINER Luecke | `rechne()` haengt „Cashback/Wechselbonus" an JEDE Zeile, deren Cashback und Wechselbonus beide None sind (`effektivpreis.py:177-178`) — und das sind alle: **52 von 52** (Luecken-Verteilung am Bestand: `{'Anschlusspreis': 40, 'Cashback/Wechselbonus': 52}`). Die Zahl war immer die Gesamtzahl und las sich als Defekt; sie ist eine Eigenschaft der Dokumentenart: Cashback/Wechselbonus steht in KEINEM Produktinformationsblatt |
| alt/neu „38 in der Karte" | `len(_karte(zeilen)["punkte"])`; Punkt nur bei `z["volumen"] and z["effektiv"] is not None and z["belastbar"]` (`tarife_view.py:173-174`) | die Karte ist eine Volumenachse: **12** Tarife nennen kein Volumen, **2** laufen unbegrenzt (kein Ort auf der Achse — `tarife_view.py:105,112`), 52−12−2 = **38**; alle 52 sind belastbar, also faellt kein furtherer Grund an |
| neu „40 ohne Anschlusspreis-Angabe" | `sum(1 for z in zeilen if "Anschlusspreis" in z["luecken"])` | die EINE zaehlbare Luecke des Bestands (`effektivpreis.py:169`) — bewusst ohne die Cashback-Luecke, die jeder traegt |

**Wahrheitstests:** `test_die_bilanz_zaehlt_was_ihr_etikett_sagt` (cccf6ba, gestellt,
incl. des Falls „Volumen bekannt, Grundpreis fehlt — Karte zaehlt anders als Tabelle")
und neu `test_die_stat_zeile_am_echten_bestand` (`1ae05f3`, F5-Muster): Cashback-Luecke
an jeder Zeile (= der Hinweissatz ist wahr), Partition „jeder Tarif genau ein Grund"
(38+12+2+0 = 52), Stat-Zahl identisch mit den gezeichneten Punkten.

## 2. S-Q4 „Bindung" — Vereinheitlichung auf den TCO24-Stand

Regel: „Bindung" (Substantiv) nennt nur noch, was ein Vertrag tatsaechlich bindet —
Tarif und Geraeteraten, als Verb direkt an der Karte („der Tarif bindet 24 Monate,
die Geräteraten laufen 36"). Der Rechnungshorizont heisst Horizont („gerechnet über
24 Monate", TCO24-1, 08.09.2026: Leitzahl konstant TCO-24, `LAUFZEIT = TCO_HORIZONT`).

**grep-Vorher/Nachher, gerenderte Seiten** (`site/`, vor `5241326~1` → nach):

| Seite | Vorher | Nachher |
|---|---|---|
| `tarife.html` | 22× „24 Monate Bindung" (Laufzeit-Merkmal) | 22× „24 Monate Mindestlaufzeit"; **0×** „Bindung" |
| `geraete.html` | 908× „Bindung": 410× „Gerechnet über 24 Monate Bindung" (davon 248× mit dem Zusatz „– der Tarif bindet 24 Monate, die Geräteraten laufen 36" — der Widerspruch im selben Satz), 88× Modellkopf „· Bindung 24", 88× Sortier-Option „TCO je Laufzeitgruppe", 61× 1&1-BAU „… zusammen · 24 Monate" (Zahlungsdauer falsch: die Raten laufen 36), 60× „(12 Geräteraten)" auch auf 1&1-Karten (deren offener Betrag ist Tarif PLUS Geraet), 162× „… dieser Bindung" | **0×** „Bindung"; 248× „– der Tarif bindet 24 Monate, die Geräteraten laufen 36" (Verb, bleibt), 88× „· gerechnet über 24 Monate", 88× „TCO-24, aufsteigend", 61× „· 36 Monate (24 davon in der TCO-24)", 61× „(12 Monatsraten)" auf 1&1 / weiterhin „(12 Geräteraten)" auf o2 |

**Zur Frage „teilt geraete.html die Chart-Überschrift"?** Ja, indirekt: die
Chart-Titel des Tickets („36 Monate Bindung") sind die G1-Gruppenkoepfe aus
`geraete_tco_grafik.balken()` — diese Funktion wird seit BRIEF_FADEN (05.09.)
bewusst nicht mehr gerendert (Kommentar `geraete.html.j2:993`), lebte aber mit
dem alten Wortlaut im Code und in zwei Tests weiter. `tarife.html` nutzt G1
nicht; sein „Bindung"-Vorkommen war das Laufzeit-Merkmal aus `effektivpreis.py`
(deshalb stehen beide Dateien in diesem Ticket). Der Site-Diff von geraete.html
(3415 Zeilen) kommt daher, dass die fuenf Wortaelnderungen auf je 60–410 Karten
fallen — klassifiziert: jede geaenderte Zeile gehoert zu einem der fuenf Muster,
keine sonstige.

**Keine Zahl falsch geworden** (dreifach belegt):
1. Suite gruen, inkl. `test_die_referenzkarte_behauptet_keine_36_monate` und der
   umgestellten Terminologie-Assertions.
2. diff-reviewer: Multiset aller Euro-Betraege, `data-*`-Attribute, Abrufdaten und
   Stand-Zeilen von `site/geraete.html` zwischen HEAD~4 und Worktree identisch;
   die 24→36 der BAU-Zeile ist Benennungskorrektur (vorher stand der Horizont als
   Zahlungsdauer).
3. `grep`-Gegenprobe: 0× alte Muster auf beiden Seiten; die einzigen neuen
   Zahlworte („36 Monate", „(24 davon in der TCO-24)", „Monatsraten") sind an
   1&1-Karten gebunden und dort wahr (35/35 1&1-Listungen mit
   `laufzeit_monate=36`; `tco_24` rechnet `min(36,24)` Raten ein,
   `restbetrag=12×Rate`).

## 3. Suite — Zahlen und jede geaenderte Test-Zeile

`PYTHONPATH=src python3 -m pytest -q` → **2884 passed, 0 failed, 12 skipped**
(Baseline 2881/0/12; Differenz: +1 `cccf6ba`, +2 R2). Nichts geschwaecht —
diff-reviewer: „die beiden umgeschriebenen Tests wurden staerker statt schwaecher;
kein Test geloescht; kein Test im Repo assertiert noch alte Wortlaute positiv."

Geaenderte/ergaenzte Test-Zeilen, alle mit Begründung:

| Datei | Aenderung | Begründung |
|---|---|---|
| `test_geraete_tco_hauptansicht.py:508-513` (R1-WIP) | `"24 Monate Bindung" in svg` → `"gerechnet über 24 Monate" in svg` + neu `assert "Monate Bindung" not in svg` | folgt dem neuen Gruppenkopf-Wortlaut; die Negativ-Assertion haelt das Substantiv draussen |
| `test_geraete_tco_hauptansicht.py:647f.` (R1-WIP) | `"24 Monate Bindung" in svg and "36 Monate Bindung" in svg` → beide „gerechnet über …" | derselbe Wortlaut im Zwei-Gruppen-Fall |
| `test_geraete_tco_terminologie.py:66-76` | `assert "Gerechnet über 24 Monate Bindung" in o2_text` → Negativ „24 Monate Bindung" + neuer Satz-Anfang + „Kosten der ersten 24 Monate" | folgt dem neuen Rechenweg-Satz; Zahl „240,00 € (12 Geräteraten)" unveraendert mitgeprueft |
| `test_geraete_tco_terminologie.py:83f.` | Sortier-Option `== "TCO je Laufzeitgruppe"` → `== "TCO-24, aufsteigend"` | Label-Umbenennung; die Sortierung selbst (erst `data-laufzeit`, dann `data-gesamt`) ist unangetastet und mit konstanten 24ern identisch mit „nach TCO-24 aufsteigend" |
| `test_geraete_tco_terminologie.py` NEU | `test_die_buendelkarte_nennt_die_wahre_dauer_und_die_richtigen_raten` | diff-reviewer S1: die neue 1&1-BAU-Zeile (neue Zahl 36 auf 61 Live-Karten) war von keinem Test gedeckt — exakter Wortlaut + „Monatsraten" nur auf der Bündelkarte, „Geräteraten" weiter auf o2 |
| `test_geraete_tco_zustand.py` (`_baue`, `_speicherform`) | Opt-in-Parameter `eins_und_eins` (1&1-Bündel ans selbe Modell, Default False — kein bestehender Aufrufer aendert sich); `_speicherform` um `buendel_monatlich` ergänzt | Fixture-Erweiterung fuer obigen Test, Bauweise wie `test_geraete_buendel_einsundeins` (§ 13.2: nur der kombinierte Betrag); `buendel_monatlich` fehlte in der Speicherform bisher |
| `test_effektivpreis.py` NEU | `test_die_stat_zeile_am_echten_bestand` | der von R1 angekuendigte, nicht angekommene Echtbestand-Test (S-Q2) |

Bewusst stehengelassene S3-Anmerkung des diff-reviewers: die beiden
Nachrechn-Zeilen (`ohne_anschlusspreis`, `ohne_volumen`) im Echtbestand-Test
reevaluieren nahe am Quell-Ausdruck — ihr Wert sind die danebenstehende
Cashback-Tripwire (sieert den Hinweissatz) und die Partition; so ist auch
`test_seiten_zahlen.py` gebaut.

## 4. Site-Rendering

`render_site(site, data/reports, load_config(root))` — der etablierte Weg mit `cfg`.

```
 git diff --stat site/ (gegen d26617e):
 site/geraete.html | 3415 ++++++++++++++++------
 site/tarife.html  |   62 +-
 2 files changed, 1920 insertions(+), 1557 deletions(-)
```

Stichproben (Vorher → Nachher, gerendert):

- tarife.html Stat-Zeile: „52 / mit offenen Punkten (z. B. Cashback)" →
  „40 / ohne Anschlusspreis-Angabe"; „38 / davon in der Karte" →
  „38 / mit Datenvolumen in der Karte"
- tarife.html Merkmal: „24 Monate Bindung" → „24 Monate Mindestlaufzeit" (22×)
- geraete.html 1&1-BAU: „monatlich 62,99 € für Tarif und Gerät zusammen ·
  24 Monate" → „… · 36 Monate (24 davon in der TCO-24)" (61×)
- geraete.html Pflichtzeile 1&1: „… 755,88 € (12 Geräteraten)" →
  „… 755,88 € (12 Monatsraten)" (61×; o2-Karten bleiben „Geräteraten")
- geraete.html Rechenweg: „Gerechnet über 24 Monate Bindung. Enthalten sind
  alle fest anfallenden Kosten dieser Bindung" → „Gerechnet über 24 Monate.
  Enthalten sind alle fest anfallenden Kosten der ersten 24 Monate; was danach
  noch läuft, steht als offener Betrag auf der Karte"

Keine Zahlen-Drifts: alle geaenderten Zeilen beider Seiten wurden maschinell in
Musterklassen zerlegt — es gibt keine Zeile ausserhalb der erwarteten fünf bzw.
drei Muster; `site/tarife.html` traegt erstmals den Render von `cccf6ba` (R1
hatte nach dem Commit nicht mehr neu gerendert). Frisches Render in einen
leeren Ordnel ist byte-identisch zum committeten Stand (diff-reviewer);
shasum nach dem letzten Render: `geraete.html 16f2017…`, `tarife.html d9ebf19…`.
Der `>`-Fix (S3) ist am echten Bestand byte-neutral — alter und neuer Render
identisch, weil alle 61 Faelle 36>24 sind; der konstruierte Fall steht im Test.

## 5. Offen / Grenzen

1. **Nicht gepusht** — der Brief verlangt „Stand gepusht auf dem Ticket-Branch
   belassen"; dieser Bericht und der Push folgen als letzter Schritt der R2-Session
   (siehe Protokoll der Session).
2. **G1 bleibt ungerendert** (BRIEF_FADEN-Entscheidung, unveraendert). Wenn G1
   zurueckkommt, stehen seine Gruppenkoepfe schon im neuen Wortlaut; die zwei
   G1-Tests halten ihn.
3. **pruefe_portal.py** wurde nicht ausgefuehrt (Vorbehalte aus CLAUDE.md zu
   `--no-llm`-Staenden gelten hier nicht, aber die Kriterien kennen die
   geaenderten Etikette nicht — diff-reviewer hat das gegengecheckt).
4. Die Sortier-Option heisst jetzt „TCO-24, aufsteigend" und das ist wahr,
   SOLANGE TCO24-1 steht (`LAUFZEIT` ist eine Konstante, keine Datenfunktion).
   Sollte die Norm je wieder variable Bindungen erlauben, gehoert das Etikett
   mit zurueck auf „je Laufzeitgruppe" — der JS-Kommentar (A5.4) beschreibt
   die Struktur weiter korrekt.
