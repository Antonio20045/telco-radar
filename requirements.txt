httpx>=0.27
# collect/http.py prueft TLS gegen DIESES Buendel statt gegen den
# Zertifikatsspeicher des Containers. Der wechselt mit dem Basis-Image des
# Runners, und am 07.08.2026 fiel die NTIA deshalb mit
# CERTIFICATE_VERIFY_FAILED aus - obwohl dieselbe URL anderswo 200 lieferte.
certifi>=2024.2
feedparser>=6.0
beautifulsoup4>=4.12
PyYAML>=6.0
Jinja2>=3.1
Markdown>=3.6
pytest>=8.0
playwright>=1.44
# report/bilder.py misst jeden Bildkandidaten, bevor es einen auswaehlt, und
# rechnet ihn auf Zeitungsmasse herunter. Ohne Pillow gaebe es weder das eine
# noch das andere - und die Titelseite haette wieder 120x90-Bilder.
Pillow>=10.0
# Nur fuer die TESTS des Signup-Dienstes (tests/test_signup_service.py). Der
# Dienst selbst laeuft auf Render aus service/signup/requirements.txt und
# zieht die Pipeline-Abhaengigkeiten nicht mit - er ist ein eigener Prozess.
# Hier stehen sie, damit sich der Test in CI nicht mangels Import
# ueberspringt: ein Skip sieht im Protokoll aus wie ein Erfolg.
fastapi>=0.115
